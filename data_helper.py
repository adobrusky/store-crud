from database.models import Person, Address, Customer, Product, Transaction
from database.database import Person, Address, Customer, Product, ProductTransaction, Transaction

# create class that has methods for getting, updating all that stuff. make it need to be initialized with a session so it can use one session